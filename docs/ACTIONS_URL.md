# URL action
URL actions sends data to a web-based app or API.

## Example
The URL example in the app goes through the following steps:
- Sends a HTTP POST request to an external web app.
- Gets an echo response with some meta data and the message that was sent.

## Use cases
- Send data to any web app or API.