# WebSocket action
Socket actions sends data to a WebSocket host on a given port.

## Example
The WebSocket example in the app goes through the following steps:
- Sends a message to a WebSocket host (ws://host:port).
- Either does a "fire and forget", waits for a given number of reply messages or times out.

## Use cases
- Send data to online users, games and socket API besed apps.