# SDK action
SDK actions calls an internal SDK function.

## Example
The SDK example in the app goes through the following steps:
- Calls an internal SDK feature.
- Returns the ourput from the SDK call.

## Use cases
- Add special SDK features into the play.
